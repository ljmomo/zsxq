针对阶段一（桌面客户端 MVP），我将为您输出两份详细文档，并保存到 `docs/` 目录下：

1.  **需求文档 (PHASE1_PRD.md)**
    *   **产品定位**：面向非技术用户的知识星球文件批量下载工具。
    *   **核心功能**：
        *   **可视化登录**：内嵌浏览器窗口扫码登录，状态自动持久化。
        *   **订阅管理**：侧边栏展示已订阅星球，支持点击切换。
        *   **任务配置**：可视化选择下载路径、设置文件过滤规则（如只下载 PDF）。
        *   **下载监控**：实时展示当前下载文件、总进度条、成功/失败日志。
    *   **交互流程**：从启动到下载完成的完整用户旅程。

2.  **技术方案文档 (PHASE1_TECH_SPEC.md)**
    *   **技术选型**：采用 **Python + PySide6 (Qt)** 方案。
        *   *理由*：开发速度快，能直接复用现有 Playwright 逻辑，无需引入 Node.js/Electron 的额外复杂度，打包体积可控。
    *   **架构设计**：
        *   **UI 层**：基于 PySide6 的主窗口、对话框和组件。
        *   **逻辑层 (Core)**：重构现有的 `zsxq_playwright.py`，剥离 CLI 代码，封装为基于信号（Signal）通信的 Worker 类。
        *   **并发模型**：GUI 主线程 + Playwright 工作线程，解决界面卡顿问题。
    *   **数据流向**：用户操作 -> UI 信号 -> 核心逻辑执行 -> 状态回调 -> UI 更新。
    *   **打包方案**：使用 PyInstaller 生成独立可执行文件。

我将先创建 `docs` 目录，然后生成这两份文档。