# 知识星球文件下载器 - 需求文档

## 1. 项目背景

知识星球是一个知识分享平台，用户可以在星球中分享文章、文件、音频等内容。本项目旨在开发一个自动化工具，帮助用户批量下载知识星球中的文件。

## 2. 功能需求

### 2.1 核心功能

#### 2.1.1 星球文件下载模式（默认模式）

- **功能描述**：自动下载当前星球中的所有文件
- **输入**：无（使用已登录的星球）
- **输出**：下载的文件保存到指定目录
- **流程**：
  1. 打开知识星球主页
  2. 检测登录状态
  3. 遍历所有文件
  4. 下载文件到指定目录

#### 2.1.2 链接解析下载模式（新增功能）

- **功能描述**：从给定的链接中解析并下载文件
- **输入**：链接列表文件（links.txt）
- **输出**：下载的文件保存到指定目录
- **流程**：
  1. 打开初始页面
  2. 提取书籍列表（过滤评论）
  3. 导航到书籍详情页
  4. 提取详情页附件（过滤评论）
  5. 点击附件，打开下载弹窗
  6. 智能识别下载按钮
  7. 点击下载按钮
  8. 使用解析的文件名保存
  9. 关闭弹窗，返回初始页面

### 2.2 文件类型过滤

- **支持的文件类型**：`.mp3`, `.doc`, `.docx`
- **过滤规则**：
  - 过滤评论内容
  - 过滤回复内容
  - 过滤时间戳格式
  - 过滤用户名格式

### 2.3 评论过滤功能

**过滤关键词**：
- 评论、回复、留言、comment、reply
- 评论区、回复区、留言区
- 评论者、回复者
- 评论加载中

**过滤规则**：
- 包含评论关键词
- 包含时间戳格式（如 2021-05-08）
- 包含用户名格式（如 "用户名："）
- 多行内容（超过3行）
- 长文本（超过200字符）

### 2.4 智能下载按钮识别

**评分规则**：

| 条件 | 得分 |
|------|------|
| 文本恰好是"下载" | 100分 |
| 文本包含"下载"且长度<20 | 80分 |
| 文本包含"下载" | 50分 |
| cursor是pointer | +10分 |
| 是button或a标签 | +10分 |

**有效下载按钮**：得分 >= 80

### 2.5 文件命名

- 使用解析出来的文件名保存
- 如果文件名没有扩展名，自动从文本中提取

## 3. 非功能需求

### 3.1 性能要求

- 支持批量下载多个文件
- 支持断点续传（如果文件已存在则跳过）
- 支持并发下载（可选）

### 3.2 可靠性要求

- 自动检测登录状态
- 自动重试失败的下载
- 自动关闭弹窗，确保后续操作正常

### 3.3 易用性要求

- 支持命令行参数配置
- 支持配置文件
- 提供详细的日志输出

## 4. 输入输出

### 4.1 输入

- **链接列表文件**：`links.txt`，每行一个链接
- **命令行参数**：
  - `--mode`：运行模式（files/links/both）
  - `--links-file`：链接列表文件路径
  - `--download-dir`：下载目录
  - `--max-links`：最大处理链接数

### 4.2 输出

- **下载文件**：保存到指定目录
- **日志输出**：控制台输出操作日志

## 5. 约束条件

- 需要在浏览器中预先登录知识星球
- 需要安装 Playwright 库
- 支持 macOS/Linux/Windows

## 6. 验收标准

- [x] 能够正确提取书籍列表
- [x] 能够正确过滤评论内容
- [x] 能够正确导航到详情页
- [x] 能够正确提取详情页附件
- [x] 能够正确点击附件
- [x] 能够正确识别下载按钮
- [x] 能够正确下载文件
- [x] 能够正确保存文件（使用解析的文件名）
- [x] 能够正确关闭弹窗
- [x] 能够处理多个文件下载
